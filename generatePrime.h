#include <iostream>
#include <cmath>
#include <time.h>
using namespace std;

int* getPrimes(int lowerBound, int upperBound);
int* sieve(int num);
int getPrime(int lowerBound,int upperBound);
int gcd(int a, int b);